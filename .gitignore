# --- Gradle ---
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
.kotlin/

# --- Android ---
*.apk
*.aab
*.ap_
*.dex
captures/
.externalNativeBuild/
.cxx/
release/

# --- Local configuration (machine-specific SDK path; never commit) ---
local.properties

# --- IDEs ---
*.iml
.idea/
.DS_Store
*.hprof

# --- Signing material ---
# A keystore must never be committed. Real projects keep it in CI secrets or a password manager;
# `keystore.properties` holds the paths and passwords and is deliberately ignored too.
*.jks
*.keystore
keystore.properties

# --- Test corpus ---
# Generated by `tools/make-test-books.py`. The generator is what belongs in version control — it is
# reviewable and reproducible — while the output is a few hundred kilobytes of binaries that would
# go stale the moment the generator changed.
/test-books/

# --- Room schema exports are NOT ignored: they are deliberately committed, so that future
#     migrations can be verified against the real previous schema. See core/core-data/schemas/.
